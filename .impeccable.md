# Lets Design Context

## Design Context

### Users
小团队（2–8 人）的程序员，每个人本地都跑着 1–N 个 coding agent (Claude Code / Codex / 未来其他)。他们在同一个 GitHub 项目上协作。使用场景包括：

- 笔记本上深度工作（写代码、review agent 的 finding、与 agent 多轮对话）
- 手机上移动办公（早晨通勤时看 agent 夜里跑完的结果、轻量回复、给 agent 派一个新任务、看队友在做什么）
- 与队友一起在同一个 channel 里和不同的 agent 协作

Job to be done：
- 在一个地方看到"项目当前所有 agent 在干什么"
- 给 agent 派任务、跟 agent 多轮对话
- 看到队友和 agent 的协作，避免重复劳动
- 与 GitHub 的代码现实保持绑定（branch / commit / PR / metric）

### Brand Personality
三个词：**engineered · calm · alive**

- engineered：精确的字体节奏、严谨的对齐、克制的颜色 —— 这是给做底层工程的人用的，每一处都要有理由
- calm：长时间盯屏不刺眼。dark 主调，accent 罕用、暖色（不是 cyan-on-dark 那种 AI slop）。空白要慷慨
- alive：但不死板 —— agent 是"队友"，状态变化要可感知（presence 动态、status pulse、新 message 动画）。区别于"运维仪表盘"的死气

Tone：跟队友在工作站旁边的小白板上写东西，不是企业 SaaS 也不是炫技作品。

### Aesthetic Direction
**主基调：奶白浅色 · 惬意 · 不刺眼**

由用户明确指定：light theme，奶白色（cream / linen），给人惬意舒服的感觉。不是企业 SaaS 那种"白底蓝调"的冷感、也不是宣纸/羊皮纸的复古信纸感、更不是医院里的灰白。

参考形态：Linear 的克制 + Things 3 的呼吸感 + Stripe / Vercel light mode 的精致 + 老 Mac OS Mojave 早期 light theme 的暖意。

- Light theme（奶白）是主，dark mode 留接口
- 一个唯一的 warm accent（琥珀/橙红），其它 UI 全部低饱和
- 中性色微微偏暖（往黄/橙方向 tint），不是冷白

**Anti-references（明确不要）**：
- 现在 web/index.html 的"调试面板感"（三个并列卡片 + 纸张色 + 没有层次）
- 任何 cyan-on-dark + 紫蓝渐变（AI slop 头号特征）
- 边框左侧 3px 色条强调（admin UI 通病）
- Hero metric 大数字 + small label 的 dashboard 套路
- Glassmorphism / blur 玻璃卡
- 把"技术感"等同于全 monospace

**Conversation 是主视图**，不是次要 tab。所有其他视图（board / agents / project）都是 conversation 的 derived view。

### Design Principles
1. **Conversation-first**：信息流是核心，board / dashboard 是"项目俯瞰"的辅助视图，不并列
2. **Typed messages 统一**：chat / status / finding / decision / system 都是同一条流上的不同类型，视觉上微妙区分，不分裂到不同 surface
3. **Mobile is first-class**：底部 tab 切换、单栏会话、composer 占满底部 —— 不是把桌面版压缩
4. **Presence is alive**：agent 状态变化（idle → working → done）要有微妙运动；但不要喧宾夺主
5. **Restraint with accent**：warm accent 只用于"需要人类决策"的位置（@提到你、agent 提问、awaiting decision），其它全部 neutral

### Typography
- Display: **Bricolage Grotesque**（Google Fonts，variable opsz，特征足但克制；不在禁用表）
- Body / UI: **Geist**（Vercel 开源，编程语境天然，干净；不在禁用表）
- Mono（仅用于 code / branch name / agent_name 徽标）: **Geist Mono**

### Color Tokens (OKLCH, light "cream" theme primary)

中性色微微往**暖黄/暖橙**方向 tint（chroma 0.005-0.01, hue 60-80），让奶白不变冷。

- bg: `oklch(0.985 0.006 75)` — 奶白主底，比纸更柔
- surface: `oklch(0.965 0.008 75)` — 卡片背景，比 bg 微暗一格
- surface-elev: `oklch(0.995 0.004 75)` — 浮起卡片（比 bg 更亮一点）
- surface-hover: `oklch(0.955 0.009 75)`
- border: `oklch(0.88 0.010 75)` — 不要纯灰，沿用暖调
- border-soft: `oklch(0.92 0.008 75)`
- text-primary: `oklch(0.22 0.010 240)` — 不用纯黑，深蓝灰，远离纯黑能让奶白不刺眼
- text-muted: `oklch(0.46 0.010 240)`
- text-dim: `oklch(0.62 0.008 240)`
- **accent (amber)**: `oklch(0.62 0.16 55)` — 浅色背景下要 desaturate + 加深，才不会发糊
- accent-soft: `oklch(0.93 0.07 70)` — 浅琥珀色 chip 背景
- accent-text: `oklch(0.42 0.16 55)` — accent 上的文字
- agent-claude: `oklch(0.52 0.14 50)` — 暖橙红
- agent-claude-bg: `oklch(0.94 0.05 50)`
- agent-codex: `oklch(0.46 0.10 155)` — 沉稳绿
- agent-codex-bg: `oklch(0.93 0.04 155)`
- human: `oklch(0.45 0.10 220)` — 蓝灰
- human-bg: `oklch(0.93 0.03 220)`
- status-online: `oklch(0.62 0.14 145)` — 绿
- status-working: `oklch(0.65 0.16 55)` — 同 accent，会脉动
- status-idle: `oklch(0.65 0.008 240)`

### Layout
- Desktop ≥1280px：三栏 `280px / 1fr / 360px`
- Tablet 768–1280px：两栏（侧栏可抽屉收起）
- Mobile <768px：单栏 + 底部 4-tab（💬 chats / 📊 board / 👥 agents / ⚙ project）

### Constraints
- Mock 阶段：纯静态 HTML/CSS/小量 JS，Tailwind via CDN，无构建工具
- 未来 production：Next.js / Remix + Tailwind + shadcn-ui (我们到时再决定)
- 必须 mobile responsive 真实可用
- 必须 WCAG AA 对比度（dark theme 容易踩雷）
